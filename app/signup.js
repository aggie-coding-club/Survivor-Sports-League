import { View, Text, Button } from 'react-native';

const SignUp = () => {
  return (
    <View>
      <Text>Sign Up</Text>
      <Button title="Sign Up" onPress={() => {}} />
    </View>
  );
}

export default SignUp;